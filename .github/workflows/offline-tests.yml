name: Offline Tests

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]
  workflow_dispatch:

jobs:
  # Code quality checks
  lint:
    name: Lint
    runs-on: arc-runner-set
    timeout-minutes: 3

    steps:
      - uses: actions/checkout@v4

      - name: Setup Bun
        uses: ./.github/actions/setup-bun
        with:
          bun-version: 1.3.6

      - name: Install dependencies
        run: bun install

      - name: Run lint
        run: bun run lint

  format-check:
    name: Format Check
    runs-on: arc-runner-set
    timeout-minutes: 3

    steps:
      - uses: actions/checkout@v4

      - name: Setup Bun
        uses: ./.github/actions/setup-bun
        with:
          bun-version: 1.3.6

      - name: Install dependencies
        run: bun install

      - name: Check formatting
        run: bun run format:check

  # ============================================
  # OFFLINE TESTS (No API credentials required)
  # ============================================

  test-daemon-integration-database:
    name: Daemon Integration (database)
    runs-on: arc-runner-set
    timeout-minutes: 3

    steps:
      - uses: actions/checkout@v4

      - name: Setup Bun
        uses: ./.github/actions/setup-bun
        with:
          bun-version: 1.3.6

      - name: Install dependencies
        run: bun install

      - name: Run integration tests (database)
        working-directory: packages/daemon
        run: bun test tests/offline/integration/database --coverage --coverage-reporter=lcov --coverage-dir=coverage

      - name: Upload to Codecov
        uses: codecov/codecov-action@v4
        continue-on-error: true
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          files: packages/daemon/coverage/lcov.info
          flags: coverage-daemon-integration-database
          fail_ci_if_error: false

  test-daemon-integration-filesystem:
    name: Daemon Integration (filesystem)
    runs-on: arc-runner-set
    timeout-minutes: 3

    steps:
      - uses: actions/checkout@v4

      - name: Setup Bun
        uses: ./.github/actions/setup-bun
        with:
          bun-version: 1.3.6

      - name: Install dependencies
        run: bun install

      - name: Run integration tests (filesystem)
        working-directory: packages/daemon
        run: bun test tests/offline/integration/filesystem --coverage --coverage-reporter=lcov --coverage-dir=coverage

      - name: Upload to Codecov
        uses: codecov/codecov-action@v4
        continue-on-error: true
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          files: packages/daemon/coverage/lcov.info
          flags: coverage-daemon-integration-filesystem
          fail_ci_if_error: false

  test-daemon-integration-git:
    name: Daemon Integration (git)
    runs-on: arc-runner-set
    timeout-minutes: 3

    steps:
      - uses: actions/checkout@v4

      - name: Setup Bun
        uses: ./.github/actions/setup-bun
        with:
          bun-version: 1.3.6

      - name: Install dependencies
        run: bun install

      - name: Run integration tests (git)
        working-directory: packages/daemon
        run: bun test tests/offline/integration/git --coverage --coverage-reporter=lcov --coverage-dir=coverage

      - name: Upload to Codecov
        uses: codecov/codecov-action@v4
        continue-on-error: true
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          files: packages/daemon/coverage/lcov.info
          flags: coverage-daemon-integration-git
          fail_ci_if_error: false

  test-daemon-integration-websocket:
    name: Daemon Integration (websocket)
    runs-on: arc-runner-set
    timeout-minutes: 3

    steps:
      - uses: actions/checkout@v4

      - name: Setup Bun
        uses: ./.github/actions/setup-bun
        with:
          bun-version: 1.3.6

      - name: Install dependencies
        run: bun install

      - name: Run integration tests (websocket)
        working-directory: packages/daemon
        run: bun test tests/offline/integration/websocket --coverage --coverage-reporter=lcov --coverage-dir=coverage

      - name: Upload to Codecov
        uses: codecov/codecov-action@v4
        continue-on-error: true
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          files: packages/daemon/coverage/lcov.info
          flags: coverage-daemon-integration-websocket
          fail_ci_if_error: false

  test-daemon-integration-rpc:
    name: Daemon Integration (rpc)
    runs-on: arc-runner-set
    timeout-minutes: 3

    steps:
      - uses: actions/checkout@v4

      - name: Setup Bun
        uses: ./.github/actions/setup-bun
        with:
          bun-version: 1.3.6

      - name: Install dependencies
        run: bun install

      - name: Run integration tests (rpc)
        working-directory: packages/daemon
        run: bun test tests/offline/integration/rpc --coverage --coverage-reporter=lcov --coverage-dir=coverage

      - name: Upload to Codecov
        uses: codecov/codecov-action@v4
        continue-on-error: true
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          files: packages/daemon/coverage/lcov.info
          flags: coverage-daemon-integration-rpc
          fail_ci_if_error: false

  test-daemon-integration-session:
    name: Daemon Integration (session)
    runs-on: arc-runner-set
    timeout-minutes: 3

    steps:
      - uses: actions/checkout@v4

      - name: Setup Bun
        uses: ./.github/actions/setup-bun
        with:
          bun-version: 1.3.6

      - name: Install dependencies
        run: bun install

      - name: Run integration tests (session)
        working-directory: packages/daemon
        run: bun test tests/offline/integration/session --coverage --coverage-reporter=lcov --coverage-dir=coverage

      - name: Upload to Codecov
        uses: codecov/codecov-action@v4
        continue-on-error: true
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          files: packages/daemon/coverage/lcov.info
          flags: coverage-daemon-integration-session
          fail_ci_if_error: false

  test-daemon-integration-components:
    name: Daemon Integration (components)
    runs-on: arc-runner-set
    timeout-minutes: 3

    steps:
      - uses: actions/checkout@v4

      - name: Setup Bun
        uses: ./.github/actions/setup-bun
        with:
          bun-version: 1.3.6

      - name: Install dependencies
        run: bun install

      - name: Run integration tests (components)
        working-directory: packages/daemon
        run: bun test tests/offline/integration/components --coverage --coverage-reporter=lcov --coverage-dir=coverage

      - name: Upload to Codecov
        uses: codecov/codecov-action@v4
        continue-on-error: true
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          files: packages/daemon/coverage/lcov.info
          flags: coverage-daemon-integration-components
          fail_ci_if_error: false

  test-daemon-integration-mcp:
    name: Daemon Integration (mcp)
    runs-on: arc-runner-set
    timeout-minutes: 3

    steps:
      - uses: actions/checkout@v4

      - name: Setup Bun
        uses: ./.github/actions/setup-bun
        with:
          bun-version: 1.3.6

      - name: Install dependencies
        run: bun install

      - name: Run integration tests (mcp)
        working-directory: packages/daemon
        run: bun test tests/offline/integration/mcp --coverage --coverage-reporter=lcov --coverage-dir=coverage

      - name: Upload to Codecov
        uses: codecov/codecov-action@v4
        continue-on-error: true
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          files: packages/daemon/coverage/lcov.info
          flags: coverage-daemon-integration-mcp
          fail_ci_if_error: false

  # Unit tests - pure logic with mocks only
  test-daemon-unit:
    name: Daemon Unit Tests
    runs-on: arc-runner-set
    timeout-minutes: 3

    steps:
      - uses: actions/checkout@v4

      - name: Setup Bun
        uses: ./.github/actions/setup-bun
        with:
          bun-version: 1.3.6

      - name: Install dependencies
        run: bun install

      - name: Run unit tests
        working-directory: packages/daemon
        run: bun test tests/offline/unit --coverage --coverage-reporter=lcov --coverage-dir=coverage
        env:
          GLM_API_KEY: ""
          CLAUDE_CODE_OAUTH_TOKEN: ""

      - name: Upload to Codecov
        uses: codecov/codecov-action@v4
        continue-on-error: true
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          files: packages/daemon/coverage/lcov.info
          flags: coverage-daemon-unit
          fail_ci_if_error: false

  test-daemon-providers:
    name: Daemon Providers
    runs-on: arc-runner-set
    timeout-minutes: 3

    steps:
      - uses: actions/checkout@v4

      - name: Setup Bun
        uses: ./.github/actions/setup-bun
        with:
          bun-version: 1.3.6

      - name: Install dependencies
        run: bun install

      - name: Run provider tests
        working-directory: packages/daemon
        run: bun test tests/offline/providers --coverage --coverage-reporter=lcov --coverage-dir=coverage

      - name: Upload to Codecov
        uses: codecov/codecov-action@v4
        continue-on-error: true
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          files: packages/daemon/coverage/lcov.info
          flags: coverage-daemon-providers
          fail_ci_if_error: false

  test-shared:
    name: Test Shared
    runs-on: arc-runner-set
    timeout-minutes: 3

    steps:
      - uses: actions/checkout@v4

      - name: Setup Bun
        uses: ./.github/actions/setup-bun
        with:
          bun-version: 1.3.6

      - name: Install dependencies
        run: bun install

      - name: Run unit tests
        working-directory: packages/shared
        run: bun test --coverage --coverage-reporter=lcov --coverage-dir=coverage

      - name: Upload to Codecov
        uses: codecov/codecov-action@v4
        continue-on-error: true
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          files: packages/shared/coverage/lcov.info
          flags: coverage-shared
          fail_ci_if_error: false

  test-cli:
    name: Test CLI
    runs-on: arc-runner-set
    timeout-minutes: 3

    steps:
      - uses: actions/checkout@v4

      - name: Setup Bun
        uses: ./.github/actions/setup-bun
        with:
          bun-version: 1.3.6

      - name: Install dependencies
        run: bun install

      - name: Run CLI tests
        working-directory: packages/cli
        run: bun test --coverage --coverage-reporter=lcov --coverage-dir=coverage

      - name: Upload to Codecov
        uses: codecov/codecov-action@v4
        continue-on-error: true
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          files: packages/cli/coverage/lcov.info
          flags: coverage-cli
          fail_ci_if_error: false

  test-web:
    name: Test Web
    runs-on: arc-runner-set
    timeout-minutes: 2

    steps:
      - uses: actions/checkout@v4

      - name: Setup Bun
        uses: ./.github/actions/setup-bun
        with:
          bun-version: 1.3.6

      - name: Install dependencies
        run: bun install

      - name: Run web tests
        working-directory: packages/web
        run: bunx vitest run --coverage --coverage.reporter=lcov --coverage.reportsDirectory=coverage

      - name: Upload to Codecov
        uses: codecov/codecov-action@v4
        continue-on-error: true
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          files: packages/web/coverage/lcov.info
          flags: coverage-web
          fail_ci_if_error: false

  typecheck:
    name: Type Check
    runs-on: arc-runner-set
    timeout-minutes: 3

    steps:
      - uses: actions/checkout@v4

      - name: Setup Bun
        uses: ./.github/actions/setup-bun
        with:
          bun-version: 1.3.6

      - name: Install dependencies
        run: bun install

      - name: Type check
        run: bun run typecheck

