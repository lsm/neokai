[test]
# Exclude temp workspace files from test discovery
exclude = [
  "tmp/**",
  "**/node_modules/**",
  "**/dist/**",
  "**/.next/**",
  "e2e/**",
  "packages/e2e/**"
]

# JSX configuration for Preact
[jsx]
runtime = "automatic"
importSource = "preact"
