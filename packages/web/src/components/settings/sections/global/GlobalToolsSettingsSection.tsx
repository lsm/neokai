/**
 * GlobalToolsSettingsSection - Global tools configuration
 *
 * Configure global tool permissions and defaults.
 *
 * TODO: Extract tools logic from GlobalToolsSettings
 */

export function GlobalToolsSettingsSection() {
	// Placeholder implementation - will be populated with tools logic from GlobalToolsSettings
	return (
		<div class="space-y-6">
			<div class="rounded-lg border border-dark-700 bg-dark-800 p-4">
				<h3 class="mb-4 text-sm font-medium text-gray-200">Global Tools Configuration</h3>
				<p class="text-sm text-gray-400">
					Global tools configuration will be migrated from the existing settings modal.
				</p>
				<p class="mt-2 text-xs text-gray-500">
					This includes system prompt preset, SDK built-in tools, and NeoKai-specific tool
					permissions.
				</p>
			</div>
		</div>
	);
}
