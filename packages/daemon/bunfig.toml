# Note: Do NOT add [test] preload here — it would apply to online tests too
# (CI runs `bun test` from this directory). Unit test preload is handled by
# scripts/test-daemon.sh via --preload flag, and by package.json test:unit script.
